# Legacy Testnet Script Archive

These deployment scripts were used for Fuji/mock testnet workflows only.
They are superseded by `script/DeployMainnet.s.sol` and are not used for production deployment.
